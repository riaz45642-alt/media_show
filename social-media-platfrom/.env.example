# Local development:
VITE_API_URL=http://localhost:5000/api

# On Netlify/production, set VITE_API_URL in the hosting environment to the
# public HTTPS URL of the deployed Express API. Never use localhost there.
